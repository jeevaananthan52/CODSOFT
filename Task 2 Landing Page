<!DOCTYPE html>
<html lang="en-US">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Unique Landing Page</title>
    <style>
        :root {
            --primary: #6C63FF;
            --secondary: #F8F8FF;
            --accent: #FF6584;
            --dark: #22223B;
            --light: #fff;
        }
        body {
            margin: 0;
            font-family: 'Segoe UI', Arial, sans-serif;
            background: var(--secondary);
            color: var(--dark);
        }
        header {
            background: var(--primary);
            color: var(--light);
            padding: 32px 0 16px 0;
            text-align: center;
            box-shadow: 0 2px 10px rgba(108,99,255,0.10);
        }
        header h1 {
            font-size: 2.8rem;
            margin: 0 0 10px 0;
            letter-spacing: 2px;
        }
        header p {
            font-size: 1.2rem;
            color: #e0e0ff;
        }
        .hero {
            display: flex;
            align-items: center;
            justify-content: space-between;
            max-width: 1100px;
            margin: 40px auto 0 auto;
            padding: 30px;
            background: var(--light);
            border-radius: 18px;
            box-shadow: 0 4px 24px rgba(108,99,255,0.07);
            gap: 40px;
        }
        .hero-text {
            flex: 1;
        }
        .hero-text h2 {
            font-size: 2.2rem;
            margin-bottom: 10px;
            color: var(--primary);
        }
        .hero-text p {
            font-size: 1.15rem;
            margin-bottom: 18px;
        }
        .cta-btn {
            background: var(--accent);
            color: var(--light);
            border: none;
            padding: 14px 32px;
            border-radius: 30px;
            font-size: 1.1rem;
            cursor: pointer;
            transition: background 0.2s;
        }
        .cta-btn:hover {
            background: #ff416c;
        }
        .hero-img {
            flex: 1;
            text-align: center;
        }
        .hero-img img {
            width: 80%;
            max-width: 320px;
            border-radius: 12px;
            box-shadow: 0 2px 18px rgba(255,101,132,0.07);
        }
        .features {
            display: flex;
            justify-content: space-between;
            gap: 24px;
            max-width: 1100px;
            margin: 50px auto;
        }
        .feature-box {
            background: var(--light);
            border-radius: 12px;
            box-shadow: 0 2px 12px rgba(34,34,59,0.06);
            padding: 28px 20px;
            flex: 1;
            text-align: center;
            transition: transform 0.2s;
        }
        .feature-box:hover {
            transform: translateY(-8px) scale(1.04);
            box-shadow: 0 6px 24px rgba(34,34,59,0.12);
        }
        .feature-box h3 {
            color: var(--primary);
            margin-bottom: 10px;
        }
        .feature-box p {
            font-size: 1.05rem;
        }
        .testimonials {
            background: var(--primary);
            color: var(--light);
            padding: 40px 0;
        }
        .testimonials h2 {
            text-align: center;
            margin-bottom: 30px;
        }
        .testimonial-list {
            display: flex;
            justify-content: center;
            gap: 30px;
            max-width: 900px;
            margin: 0 auto;
        }
        .testimonial {
            background: var(--light);
            color: var(--dark);
            border-radius: 10px;
            padding: 20px;
            width: 260px;
            box-shadow: 0 2px 12px rgba(34,34,59,0.08);
            font-style: italic;
            position: relative;
        }
        .testimonial::before {
            content: "“";
            font-size: 2.5rem;
            color: var(--primary);
            position: absolute;
            left: 12px;
            top: 0;
        }
        .testimonial span {
            display: block;
            margin-top: 12px;
            font-weight: bold;
            color: var(--accent);
        }
        footer {
            background: var(--dark);
            color: var(--light);
            text-align: center;
            padding: 18px 0;
            font-size: 1rem;
            margin-top: 40px;
        }
        @media (max-width: 900px) {
            .hero, .features {
                flex-direction: column;
                gap: 32px;
            }
            .testimonial-list {
                flex-direction: column;
                gap: 22px;
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>PixelWave</h1>
        <p>Where Creativity Meets Technology</p>
    </header>

    <section class="hero">
        <div class="hero-text">
            <h2>Launch Your Next Big Idea</h2>
            <p>
                PixelWave helps you build stunning, responsive websites in minutes. 
                No coding experience needed—just your imagination!
            </p>
            <button class="cta-btn">Get Started</button>
        </div>
        <div class="hero-img">
            <img src="https://images.unsplash.com/photo-1461749280684-dccba630e2f6?auto=format&fit=crop&w=400&q=80" alt="Creative Workspace">
        </div>
    </section>

    <section class="features">
        <div class="feature-box">
            <h3>Easy Customization</h3>
            <p>Change layouts, colors, and content effortlessly to match your brand and style.</p>
        </div>
        <div class="feature-box">
            <h3>Responsive Design</h3>
            <p>Your site looks perfect on every device, from desktop to mobile.</p>
        </div>
        <div class="feature-box">
            <h3>Lightning Fast</h3>
            <p>Optimized for speed, so your visitors never have to wait.</p>
        </div>
    </section>

    <section class="testimonials">
        <h2>What Our Users Say</h2>
        <div class="testimonial-list">
            <div class="testimonial">
                PixelWave made it so easy to launch my portfolio. The design is beautiful and the process was fun!
                <span>- Jamie L.</span>
            </div>
            <div class="testimonial">
                I love how everything just works on my phone and laptop. Highly recommended for beginners!
                <span>- Alex P.</span>
            </div>
            <div class="testimonial">
                The customization options are endless. My website finally feels like me!
                <span>- Morgan S.</span>
            </div>
        </div>
    </section>

    <footer>
        &copy; 2025 PixelWave. Crafted with passion and creativity.
    </footer>
</body>
</html>
